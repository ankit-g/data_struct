#include "../include/stack.h"
#include "../include/header.h"

// supply problem instance 
int problem(char *str)
{
	init_stk(0);
	stk_pop(NULL);
	stk_push(NULL, NULL);
	clear_stk(NULL);

	return EXIT_SUCCESS;		
}
